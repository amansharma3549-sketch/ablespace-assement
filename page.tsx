'use client';
import {useEffect,useState} from 'react';
import {CheckCircle2,ChevronDown,Plus,Search,UserCircle2} from 'lucide-react';
import Sidebar from '../components/Sidebar';import ThemeToggle from '../components/ThemeToggle';import {createTask,getTasks,toggleTask} from '../lib/api';
type Task={id:number;title:string;description?:string;status:string;createdAt:string};
export default function HomePage(){const [guest,setGuest]=useState(false);const [tasks,setTasks]=useState<Task[]>([]);const [loading,setLoading]=useState(false);const [query,setQuery]=useState('');const [show,setShow]=useState(false);const [title,setTitle]=useState('');const [desc,setDesc]=useState('');
useEffect(()=>{setGuest(localStorage.getItem('guest')==='1')},[]);useEffect(()=>{if(guest)getTasks().then(setTasks).catch(()=>{})},[guest]);
if(!guest)return <main className="flex min-h-screen items-center justify-center p-6"><div className="panel w-full max-w-md rounded-2xl p-8 shadow-sm"><div className="mb-8 text-center"><div className="mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-2xl bg-brand text-white"><CheckCircle2/></div><h1 className="text-2xl font-bold">Welcome to AbleSpace</h1><p className="muted mt-2 text-sm">Task Management System</p></div><button onClick={()=>{localStorage.setItem('guest','1');setGuest(true)}} className="w-full rounded-xl bg-brand py-3 font-semibold text-white hover:opacity-90">Continue as Guest</button><p className="muted mt-4 text-center text-xs">No account required for this assessment demo.</p></div></main>;
const filtered=tasks.filter(t=>t.title.toLowerCase().includes(query.toLowerCase()));async function add(){if(!title.trim())return;setLoading(true);try{const t=await createTask(title,desc);setTasks([t,...tasks]);setTitle('');setDesc('');setShow(false)}finally{setLoading(false)}}async function complete(t:Task){const s=t.status==='DONE'?'TODO':'DONE';const updated=await toggleTask(t.id,s);setTasks(tasks.map(x=>x.id===t.id?updated:x))}
return <div className="flex min-h-screen"><Sidebar/><main className="min-w-0 flex-1"><header className="panel flex h-16 items-center justify-end gap-3 border-x-0 border-t-0 px-4 lg:px-8"><ThemeToggle/><button className="flex items-center gap-2 rounded-lg px-2 py-1"><UserCircle2 size={26}/><span className="hidden text-sm sm:block">Guest User</span><ChevronDown size={15}/></button></header><section className="mx-auto max-w-7xl p-4 sm:p-6 lg:p-8"><div className="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="muted text-sm">Workspace / Tasks</p><h1 className="mt-1 text-2xl font-bold">Task Management</h1></div><button onClick={()=>setShow(true)} className="flex items-center justify-center gap-2 rounded-lg bg-brand px-4 py-2.5 text-sm font-semibold text-white"><Plus size={17}/>Add Task</button></div><div className="panel rounded-xl"><div className="flex flex-col gap-3 border-b p-4 sm:flex-row sm:items-center sm:justify-between"><div className="relative w-full sm:max-w-sm"><Search className="absolute left-3 top-2.5 muted" size={17}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search tasks..." className="input w-full rounded-lg py-2 pl-9 pr-3 text-sm outline-none"/></div><span className="muted text-sm">{filtered.length} tasks</span></div><div className="divide-y dark:divide-gray-800">{filtered.length===0?<div className="p-10 text-center text-sm muted">No tasks found. Create your first task.</div>:filtered.map(t=><div key={t.id} className="flex items-center gap-3 p-4 hover:bg-gray-50 dark:hover:bg-gray-900"><button onClick={()=>complete(t)} className={`h-5 w-5 shrink-0 rounded-full border-2 ${t.status==='DONE'?'border-green-500 bg-green-500':'border-gray-300'}`}>{t.status==='DONE'&&<span className="block text-xs text-white">✓</span>}</button><div className="min-w-0 flex-1"><p className={`truncate text-sm font-medium ${t.status==='DONE'?'line-through muted':''}`}>{t.title}</p><p className="muted truncate text-xs">{t.description||'No description'}</p></div><span className={`hidden rounded-full px-2.5 py-1 text-xs sm:block ${t.status==='DONE'?'bg-green-50 text-green-700':'bg-purple-50 text-purple-700'}`}>{t.status==='DONE'?'Completed':'To do'}</span></div>)}</div></div></section></main>{show&&<div className="fixed inset-0 z-40 flex items-center justify-center bg-black/40 p-4"><div className="panel w-full max-w-md rounded-2xl p-6"><h2 className="text-lg font-bold">Add Task</h2><input autoFocus value={title} onChange={e=>setTitle(e.target.value)} placeholder="Task title" className="input mt-5 w-full rounded-lg p-3 text-sm outline-none"/><textarea value={desc} onChange={e=>setDesc(e.target.value)} placeholder="Description (optional)" className="input mt-3 h-24 w-full rounded-lg p-3 text-sm outline-none"/><div className="mt-5 flex justify-end gap-2"><button onClick={()=>setShow(false)} className="rounded-lg border px-4 py-2 text-sm">Cancel</button><button disabled={loading} onClick={add} className="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white">{loading?'Saving...':'Create Task'}</button></div></div></div>}</div>}
